<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
  android:layout_width="match_parent" android:layout_height="match_parent">

  <LinearLayout android:orientation="vertical" android:padding="16dp"
    android:layout_width="match_parent" android:layout_height="wrap_content">

    <TextView android:text="Preferred torrent download client"/>
    <Spinner android:id="@+id/clientSpinner"
      android:layout_width="match_parent" android:layout_height="wrap_content"/>

    <TextView android:text="Add custom provider (JSON)"/>
    <EditText android:id="@+id/providerJsonInput"
      android:hint="{ id, name, searchUrl, parser, headersProfile, proxyProfile, timeoutMs }"
      android:layout_width="match_parent" android:layout_height="150dp"
      android:gravity="top" android:inputType="textMultiLine"/>

    <Button android:id="@+id/addProviderBtn" android:text="Add Provider"
      android:layout_width="wrap_content" android:layout_height="wrap_content"/>
  </LinearLayout>
</ScrollView>
